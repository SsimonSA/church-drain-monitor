# 3D models

Copies of the printable parts, taken from `Dropbox\3D Objects` in September 2026.
Edit the originals there in your slicer; refresh these copies when they change.

| File | Part |
|---|---|
| `Drain Float.3mf` | Float that rides inside the 2" PVC standpipe (2026-07-01) |
| `Drain Tube Liner.3mf` | Liner for the standpipe bore (2026-07-01) |
| `Dist Sensor Cap.3mf` | End cap holding the HC-SR04 at the top of the pipe (2026-06-02) |

The display enclosure and lid were also printed, but the matching files in
`3D Objects` are not named for this project (candidates: `Circuit Board Enclosure`,
`Enclosure Cover`, `Sensor Enclosure`, `Cap V2`). Copy them here once identified.
